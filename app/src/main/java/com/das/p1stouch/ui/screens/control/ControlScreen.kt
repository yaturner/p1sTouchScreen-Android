package com.das.p1stouch.ui.screens.control

import androidx.compose.runtime.Composable
import com.das.p1stouch.ui.components.PlaceholderScreen

// TODO(M6): jog pad + home, step size, extrude/retract via ValueEntryDialog,
// nozzle/bed temp, fan speeds, light toggle -- port of ui/screens/control.py,
// verified against real hardware with small (0.1mm) jog steps first.
@Composable
fun ControlScreen() {
    PlaceholderScreen("Control")
}
