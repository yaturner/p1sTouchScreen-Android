package com.das.p1stouch.ui.screens.printmonitor

import androidx.compose.runtime.Composable
import com.das.p1stouch.ui.components.PlaceholderScreen

// TODO(M5): live camera feed, progress/layers/ETA, pause/resume/stop, speed
// dropdown -- port of ui/screens/print_monitor.py, built after the camera
// stream protocol is verified against real hardware in isolation.
@Composable
fun PrintMonitorScreen() {
    PlaceholderScreen("Print Monitor")
}
